{
  "name": "recall-site",
  "version": "1.0.0",
  "description": "RECALL documentation site — built in RECALL",
  "private": true,
  "scripts": {
    "build": "recall build src/ --out public/ && cp static/_redirects public/_redirects && cp static/playground.html public/playground.html"
  },
  "dependencies": {
    "@semanticintent/recall-compiler": "^1.0.7"
  }
}
